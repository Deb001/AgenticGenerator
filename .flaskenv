# .flaskenv - Environment variables for Flask development

# The entry point of the application
FLASK_APP=app.py

# Set environment to development for debug mode and auto-reload
FLASK_ENV=development

# Optional: set the port (default 5000)
# FLASK_RUN_PORT=8000

# Optional: enable debug mode explicitly (overrides FLASK_ENV)
# FLASK_DEBUG=1