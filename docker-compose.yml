version: "3.8"

services:
  web:
    container_name: web_calculator
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env.example
    environment:
      - FLASK_APP=app.py
    volumes:
      - .:/app
    command: flask run --host=0.0.0.0
    restart: unless-stopped