{
  "name": "calculator-app",
  "version": "1.0.0",
  "description": "A simple web calculator with unit and end-to-end tests.",
  "private": true,
  "scripts": {
    "test": "jest",
    "e2e": "playwright test",
    "serve": "serve -s src -l 5000",
    "ci": "npm run test && npm run e2e"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "@playwright/test": "^1.40.0",
    "serve": "^14.2.0"
  }
}